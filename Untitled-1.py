class Solution:

    class Stack:
        top = None
        def __init__(self, top):
            self.top = top

        class Node:
            next = None
            def __init__(self, data,):
                self.data = data
                self.next = next    

    def isValid(self, s: str) -> bool:

        new_stack = self.Stack
